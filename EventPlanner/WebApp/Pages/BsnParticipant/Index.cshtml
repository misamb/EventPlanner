@page
@model WebApp.Pages_BisParticipant.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BusinessParticipant[0].Business)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BusinessParticipant[0].AdditionalInfo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BusinessParticipant[0].Event)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BusinessParticipant[0].ParticipantCount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.BusinessParticipant) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Business!.BusinessName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdditionalInfo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Event!.EventLocation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ParticipantCount)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
